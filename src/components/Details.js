import React, { useState, useEffect } from "react";
import { API_KEY } from "../constants";
import axios from "axios";

const Details = (props) => {
  return <h1>Whats poppin</h1>;
};

export default Details;
